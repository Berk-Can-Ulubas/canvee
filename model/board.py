class Board:
    def __init__(self):
        # initialize roots of the board
        self.fist_base_red = Base(None, Color.RED)
        self.fist_base_green = Base(None, Color.GREEN)
        self.fist_base_yellow = Base(None, Color.YELLOW)
        self.fist_base_blue = Base(None, Color.BLUE)

        # create graph of the board

        # reference points of start and home for each color


